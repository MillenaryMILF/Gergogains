# OS cruft
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Never commit secrets. There are none in this repo by design —
# if you ever add a build step that needs a key, put it here.
.env
.env.*
*.pem
*.key
secrets.json

# Dependencies / build output (this site needs no build step)
node_modules/
dist/
build/
